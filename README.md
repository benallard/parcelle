# parcelle

**Working the Land** — an interactive field path planning experience.

A field. A machine. A task. Every field is different. Every pass is a decision.

---

Built with vanilla HTML/CSS/JS, no dependencies.  
Published via GitHub Pages (`gh-pages` branch).

&copy; parcelle contributors
